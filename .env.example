# Copy to .env (gitignored).

OPENAI_API_KEY=          # GPT-4o baseline contestant
RUNPOD_API_KEY=          # GPU pods for training + serving
HF_TOKEN=                # Hugging Face (dataset streaming / model pulls)

# Local vLLM completion endpoint during eval (GPU box)
VLLM_BASE_URL=http://localhost:8000/v1

# Path to the Stockfish binary (auto-detected if on PATH)
STOCKFISH_PATH=
